apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart-telemetry-service.fullname" . }}
  labels:
    {{- include "smart-telemetry-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smart-telemetry-service.selectorLabels" . | nindent 4 }}
